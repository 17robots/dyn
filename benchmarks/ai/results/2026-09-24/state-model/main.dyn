pub enum Status { Pending, Complete: usize, Failed: isize }

pub fn amount(status: Status) usize {
  case status {
    Status.Pending => { return 0 },
    Status.Complete value => { return value },
    Status.Failed _ => { return 0 },
  }
}

fn main() {}
