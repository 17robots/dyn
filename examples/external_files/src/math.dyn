/*
so there are a couple ways we can export items in separate files

*/

// one way is to export each thing

// could also just put this at the top of each file to let them know it can export all items in the file

export *;

i32 addTwo(i32 a, i32 b) { a + 2 }

i32 addOne(i32 a) { a + 1 }

// do we want default exports

// should be able to import * from something and create hoisted exports for libs