//! Pattern parsing is implemented in parser.zig for now because it shares parser state.
//! This module marks Step 5's pattern-parser boundary for later extraction.

pub const PatternParserLivesInParser = void;
