module main

x := 1
x = "s"

y := unknown_name + 1
z := if 1 { 2 } else { 3 }
w := true && 1

break
continue

not_fn := 42
bad_call := not_fn()

f := (a: i32) i32 => a
bad_arg_type := f("x")
bad_arg_count := f(1, 2)

main := () i32 => 0
