module main

a: i32 = "x"
b: bool = 1

num_bad := 1 + "s"
cmp_bad := "a" < 1
eq_bad := 1 == "1"
logic_bad := 1 && (1 < 2)
unary_bad := !1

f := (x: i32, y: bool) i32 => x
call_arg_type_bad := f("x", 1)

g := (x: i32) i32 => x
call_arg_count_bad := g(1, 2)

assign_bad := 10
assign_bad = "x"
