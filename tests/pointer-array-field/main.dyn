struct Table { values: [4]u32 }

fn set(table: *Table, index: usize, value: u32) {
  table.values[index] = value
}

fn main() {
  table := Table{}
  set(&table, 2, 42)
  if table.values[0] != 0 || table.values[2] != 42 { #panic("pointer array field") }
}
