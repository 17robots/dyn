fn cleanup(message: []const u8) {
  _ = #syscall(1, 2, &message[0], #len(message))
}

fn fail() {
  defer cleanup("cleanup 3\n")
  #panic("expected")
}

fn middle() {
  defer cleanup("cleanup 2\n")
  fail()
}

fn main() {
  defer cleanup("cleanup 1\n")
  middle()
}
