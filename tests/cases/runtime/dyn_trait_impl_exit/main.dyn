module main

dyn := use "std/dyn"

Speaker := dyn.Trait(struct {
    speak: (*any) i32,
})

Dog := struct {}

Dog.speak := (self: *any) i32 => 7

main := () i32 {
    dog := Dog{}
    speaker: Speaker = dyn.impl(Speaker, Dog, &dog)
    return speaker.speak()
}
