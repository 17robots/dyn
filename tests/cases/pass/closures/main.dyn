module main

apply := (x: i32, f: (v: i32) i32) i32 => f(x)

make_adder := (n: i32) (x: i32) i32 => (x: i32) i32 => x + n

main := () i32 {
    double := (x: i32) i32 => x * 2
    result := apply(5, double)
    _ = result
    add3 := make_adder(3)
    sum := add3(7)
    _ = sum
    return 0
}
