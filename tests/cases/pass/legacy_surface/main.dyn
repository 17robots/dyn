module main

write := extern (fd: i32, ptr: *u8, len: usize) i32 = "dynrt_fd_write"

Packed := packed struct {
  byte: u8,
}

Flags := enum(u8) {
  A,
  B,
}

main := () {
  p := Packed{ byte: 1 }
  p.byte
}
