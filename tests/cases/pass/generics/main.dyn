module main

Pair := (A: comp type, B: comp type) type => struct {
    first:  A,
    second: B,
}

identity := (T: comp type, x: T) T => x

main := () i32 {
    p := Pair(i32, i32).{ first: 42, second: 7 }
    _ = p.first
    _ = p.second

    n := identity(i32, 7)
    _ = n

    return 0
}
