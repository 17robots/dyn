module main

main := () i32 {
  S := struct { value: i32 }
  S.new := (value: i32) S => S{ value: value }
  S.bump := (self: S) i32 => self.value + 1

  s := S.new(6)
  if s.bump() != 7 return 1
  return 0
}
