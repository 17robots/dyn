module main

{ Allocator, Arena, eql } := use "std/mem"

main := () i32 {
    mut arena := Arena.init()
    alloc: Allocator = arena.allocator()

    bytes_a := "dyn"
    bytes_b := "dyn"
    if !eql(u8, bytes_a, bytes_b) {
        return 1
    }

    arena.deinit()
    _ = alloc
    return 0
}
