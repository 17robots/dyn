module main

Counter := struct {
    count: usize,
}

Counter.zero: Counter = .{ count: 0 }
Counter.init := () Counter => .{ count: 0 }
Counter.value := (self: Counter) usize => self.count

main := () i32 {
    c := Counter.init()
    _ = c.value()
    _ = Counter.zero
    return 0
}
