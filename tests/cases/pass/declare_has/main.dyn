module main

MakeBox := () type {
    T := struct { value: i32 }
    $declare(T, "get", (self: T) i32 => 7)
    break T
}

Box := MakeBox()

main := () i32 {
    if !$has(Box, "get") return 1
    return 0
}
