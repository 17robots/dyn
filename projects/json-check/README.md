# json-check

Small allocation-explicit JSON validator. It demonstrates process arguments, an OS-backed arena,
file input, ordinary boolean failure, and procedural terminal output.

```sh
./build/dyn build projects/json-check --release --output build/json-check
./build/json-check some.json
```

