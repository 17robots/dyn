#include "parser.h"
#include "lexer.h"

// shift reduce parser
// shift: advances the input stream by one symbol, shifted symbol becomes a new single node parse tree
// reduce: applies a completed grammar rule to some of the recent parse trees, joining them together as one tree with a new root symbol
namespace Parser {
  
};
